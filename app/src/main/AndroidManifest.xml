<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.tronku.miwokapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Phrases" android:label="Phrases" android:theme="@style/phrases"></activity>
        <activity android:name=".Colours" android:label="Colours" android:theme="@style/color" />
        <activity android:name=".Family" android:label="Family" android:theme="@style/family"/>
        <activity android:name=".Numbers" android:label="Numbers" android:theme="@style/number"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>